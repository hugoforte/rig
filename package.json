{
  "name": "rig",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cross-repo work harness. Zero dependencies.",
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "node --test"
  }
}
