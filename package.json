{
  "name": "rig",
  "version": "3.3.0",
  "private": true,
  "type": "module",
  "description": "Cross-repo work harness. Zero dependencies.",
  "engines": { "node": ">=18" },
  "bin": { "rig": "bin/rig.mjs" },
  "scripts": {
    "test": "node --test"
  }
}
